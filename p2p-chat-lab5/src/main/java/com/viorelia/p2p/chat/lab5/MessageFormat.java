package com.viorelia.p2p.chat.lab5;

/**
 * Message format enumeration for bonus points support.
 */
public enum MessageFormat {
    RAW,     // Raw strings
    JSON,    // JSON format (bonus)
    PROTOBUF // Protobuf format (bonus)
}

